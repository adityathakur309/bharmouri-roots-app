/**
 * @deprecated Prefer `getEstimateProvider()` / `shippingService`.
 * Live create/AWB/track are intentionally not exposed here.
 */
export { shiprocketProvider as shiprocketClient } from "./providers/shiprocket.provider";
export { ShiprocketProvider } from "./providers/shiprocket.provider";
